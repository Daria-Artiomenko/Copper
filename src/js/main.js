import slider from './modules/slider';

window.addEventListener('DOMContentLoaded', () =>{
    'use strict';

    slider('.best-slide', '.best-next', '.best-prev', '.slider__pagination-item', 'slider__pagination-active');

});
